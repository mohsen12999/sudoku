import React from "react";

interface IStateProps {
  }

export default class MainPage extends React.Component<IStateProps, any> {
    render():JSX.Element {
        return (
            <div>
                this is main page
            </div>
        );
    }
}
