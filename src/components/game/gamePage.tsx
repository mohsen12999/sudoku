import React from "react";

interface IStateProps {
  }

export default class GamePage extends React.Component<IStateProps, any> {
    render():JSX.Element {
        return (
            <div>
                this is game page
            </div>
        );
    }
}
